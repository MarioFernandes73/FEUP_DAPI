To run this example simply run on a terminal

docker-compose up
python populate_es.py
python server.py

Needed dependencies:
docker installed
python installed
python packages installed ( pip install elasticsearch )